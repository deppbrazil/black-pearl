import React from 'react';
import { render } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';
import <%= props.name %> from '../lib';

describe('Testing <%= props.name %> Component', () => {
  it('should render correctly', () => {});
});
