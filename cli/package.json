{
  "name": "blackpearl-cli",
  "version": "0.1.0",
  "description": "Black Pearl CLI",
  "private": true,
  "bin": {
    "blackpearl": "bin/blackpearl"
  },
  "scripts": {
    "format": "prettier --write **/*.{js,json}",
    "test": "jest",
    "watch": "jest --watch",
    "snapupdate": "jest --updateSnapshot",
    "coverage": "jest --coverage"
  },
  "files": [
    "src",
    "bin"
  ],
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "2.731.0",
    "gluegun": "4.3.1"
  },
  "devDependencies": {
    "jest": "^24.1.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}